<section class="dashboard">
    <h2 class="dashboard-head">Dashboard</h2>
    <p class="subtitle">See all your Posts </p>
    <div class="posts">
        {{#each posts}}
        {{> dashPostInfo }}
        {{/each}}
    </div>
</section>